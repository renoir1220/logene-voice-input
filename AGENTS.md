# Project Requirements

## ASR Hotword Requirement

- 本项目本地 ASR 模型必须支持热词注入。
- 不支持热词注入的模型不得作为可选项展示，也不得用于识别流程。
- 如新增模型，必须在接入时明确其热词注入能力并通过验证后方可启用。

## Problem-Solving Policy

- 遇到问题优先解决根因，不用降级策略掩盖问题。
- 不要通过“换一种实现方式绕过去”来回避当前问题。
- 如确有充分理由需要降级、绕行或切换方案，必须先向用户说明理由并取得确认后再执行。
